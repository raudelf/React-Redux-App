
export const initialState = {
    isFetching: false,
    catImg: '',
    error: ''
}

export const catImgReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}